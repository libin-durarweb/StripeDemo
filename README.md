# StripeDemo

A simple Android app demonstrating Stripe payment integration.

**Important:** Before running this app, make sure to replace the placeholder keys in the `MainActivity.kt` file with your actual Stripe API keys. Specifically, update the `paymentIntentClientSecret`, `customerId`, `ephemeralKeySecret`, and `publishableKey` variables in the `validateFromServer()` function.
